{
  "name": "superjson-types",
  "version": "0.0.4",
  "description": "Accurate serialization and deserialization types for SuperJSON.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "lint": "eslint src",
    "format": "prettier --write src",
    "prepublish": "pnpm format && pnpm lint && pnpm build"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "superjson"
  ],
  "author": "jacobofbrookklyn",
  "license": "ISC",
  "dependencies": {
    "superjson": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "@typescript-eslint/eslint-plugin": "^5.47.0",
    "@typescript-eslint/parser": "^5.47.0",
    "eslint": "^8.30.0",
    "prettier": "^2.8.1",
    "typescript": "^4.9.4"
  }
}
